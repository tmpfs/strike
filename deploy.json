{
	"host": "cyberfunk@jet.freeformsystems.com",
	"directory": "~/www",
	"commands": {
		"local": {
			"pre": [								
				{
					"command": "pwd"
				}
			],
			"post": [
				{
					"command": "echo",
					"options": [ "deployment complete $deploy_name $target $staging $script $deploy_target $logging $deploy_type $profile $deploy_file_name" ]
				}
			]
		}
	},
	"profiles": {
		"test":{ "type": "git" },
		"stage":{ "type": "git" },
		"development":{
			"type": "git",
			"local": true,
			"commands": {
				"local": {
					"pre": [
						{
							"command": "echo",
							"options": [ "hi" ]
						}
					]
				}
			}			
		},
		"production":{ "type": "git" }
	}
}
