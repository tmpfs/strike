{
	"host": "cyberfunk@jet.freeformsystems.com",
	"directory": "~/www",
	"commands": {
		"local": {
			"pre": [
				{
					"command": "pwd"
				},
				{
					"command": "ls",
					"options": [ "-la", "lib man" ]
				}				
			],
			"post": [
				{
					"command": "echo",
					"options": [ "deployment complete (command)" ]
				}
			]
		}
	},
	"profiles": {
		"test":{ "type": "git" },
		"stage":{ "type": "git" },
		"development":{
			"type": "git",
			"local": true,
			"commands": {
				"local": {
					"pre": [
						{
							"command": "echo",
							"options": [ "hi" ]
						}
					]
				}
			}			
		},
		"production":{ "type": "git" }
	}
}
