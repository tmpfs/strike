{
	"host": "cyberfunk@jet.freeformsystems.com",
	"directory": "~/www",
	"commands": {
		"local": {
			"pre": [								
				{
					"command": "pwd"
				}
			],
			"post": [
				{
					"command": "echo",
					"options": [ "deployment complete $noop $flat $include_version $undefined" ]
				}
			]
		}
	},
	"profiles": {
		"test":{ "type": "git" },
		"stage":{ "type": "git" },
		"development":{
			"type": "git",
			"local": true,
			"commands": {
				"local": {
					"pre": [
						{
							"command": "echo",
							"options": [ "hi" ]
						}
					]
				}
			}			
		},
		"production":{ "type": "git" }
	}
}
