require 'json';

function tasks.npm.package.print {
	local descriptor="${process_dirs[root]}/package.json";
	
	# parse the json package descriptor
	json.parse < "$descriptor";
	json.print;
	
	# json.parse <<< '{ "data": "value" }';
	# json.print;
}